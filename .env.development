# DO NOT ADD SECRETS TO THIS FILE. This is a good place for defaults.
# If you want to add secrets use `.env.local` instead.

# These variables would be used at build time, if NODE_ENV === development.
# The problem is that the Dockerfile will always be built with `ENV NODE_ENV production`
# which means these values will never be used when building the Dockerfile.

BUILDTIME_SERVER_VARIABLE="dev_server_buildtime"

BUILDTIME_CLIENT_VARIABLE="dev_client_buildtime"
